import { default as multiply, message } from "./module-01.js";

console.log(multiply (21, 2));    // 42
console.log(message);             // "42 is the answer to the everything."

// babel-node --harmony module-06.js
